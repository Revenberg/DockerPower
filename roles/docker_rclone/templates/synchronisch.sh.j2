#!/bin/bash

RCLONE_CONFIG=/root/.rclone.conf
export RCLONE_CONFIG

while :
do 
    pause 60
    rclone copy /Dropbox DropBox:Apps/rev_rpi_rclone/backup --log-level INFO --auto-confirm --error-on-no-transfer --check-first
done