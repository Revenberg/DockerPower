--- 
- name: get facts
  include_tasks: get_facts.yaml

- name: create /var/docker-compose/data/rclone/config
  file:
    path: "{{  item }}"
    recurse: yes
    mode: 0755
    owner: pi
    group: pi
  with_items:         
  - "/var/docker-compose/data/rclone/config"

- name: Template alertmanager files
  template:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    mode: 0755
    owner: pi
    group: pi
  with_items:
  - { src: "rclone.j2", dest: "/var/docker-compose/data/rclone/config/rclone" }
  
- name: add to docker-compose
  include_tasks: docker-compose.yaml
